Windows Client

1. 安裝python依賴套件(cmd請先cd移動到跟requirements.txt同一層的資料夾)：pip3 install -r requirements.txt
※有兩個檔案一個是需要可以直接連線到Ollama Server,另一個是透過Open Webui去連線(需要去設定 -> 帳戶 -> 新增API)


2.config.ini檔案需和.py執行檔放在同一資料夾下，並依照需求更改內容。



3.jenkins執行的command是mlflow_auto_test.py的路徑
