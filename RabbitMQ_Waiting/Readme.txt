Windows Client

1.安裝python依賴套件(cmd請先cd移動到跟requirements.txt同一層的資料夾)：pip install -r requirements.txt



2.修改config.ini檔案，且需和.py執行檔放在同一資料夾下，並依照需求更改內容。
修改以下參數:
RabbitMQ_ip 	=>修改為RabbitMQ網路位置
Port		=>修改為amqp的Port預設值是5762
Username	=>除非有改預設帳號，不然通常是不用改
Password	=>除非有改預設密碼，不然通常是不用改


